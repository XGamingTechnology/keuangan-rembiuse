version: "3.8"

services:
  # 🗄️ PostgreSQL Database (Containerized)
  db:
    image: postgres:16-alpine
    container_name: office_admin_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./backups:/backups
    networks:
      - office_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 🔌 Backend NestJS
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: office_admin_backend
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN}
      - NODE_ENV=production
      - PORT=${BACKEND_PORT}
      - CORS_ORIGIN=https://office.getopurtunity.online
    # ✅ EXPOSE PORT KE 127.0.0.1 SAJA (Aman untuk Caddy)
    ports:
      - "127.0.0.1:${BACKEND_PORT:-3000}:${BACKEND_PORT:-3000}"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - office_network
    command: sh -c "sleep 5 && node dist/main"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${BACKEND_PORT:-3000}/api/office/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # 🎨 Frontend React + Vite + Nginx
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: office_admin_frontend
    restart: unless-stopped
    environment:
      - VITE_API_URL=https://office.getopurtunity.online/api/office
    # ✅ EXPOSE PORT KE 127.0.0.1 SAJA (Caddy → localhost:5173 → container:80)
    ports:
      - "127.0.0.1:5173:80"
    depends_on:
      - backend
    networks:
      - office_network

volumes:
  db_data:

networks:
  office_network:
    driver: bridge
